//! Shared configuration contracts for schema metadata and defaults.

mod path;
mod schema;

pub use path::Path;
pub use schema::ConfigSchema;
pub use schema::Field;
pub use schema::FieldKind;
pub use schema::Schema;
pub use schema::SchemaBuilder;
